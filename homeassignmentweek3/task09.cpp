#include<iostream>
using namespace std;
int main(){
    int a,sum,b,c,d;
    cout<<"Enter a 4-digit number";
    cin>>a;
    sum=(a%10)+(b%10)+(c%10)+(d%10);
    cout<<"The summ of individual digits is : "<<sum;
}